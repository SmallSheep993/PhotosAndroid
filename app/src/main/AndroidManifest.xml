<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.photosandroid">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhotosAndroid"
        tools:targetApi="31">

        <!-- 🔰 Launch HomeActivity -->
        <activity
            android:name=".view.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 📁 Album View -->
        <activity android:name=".view.AlbumActivity" />

        <!-- 🎞️ Slideshow View -->
        <activity android:name=".view.PhotoSlideshowActivity" />

        <!-- 🔍 Tag-based Search (You forgot this one!) -->
        <activity android:name=".view.SearchActivity" /> <!-- ✅ 添加了这一行 -->

    </application>

</manifest>
